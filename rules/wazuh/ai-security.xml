<!--
  Wazuh SIEM Alert Rules — ai-security
  Total rules: 27
-->
<group name="wazuh,ai,security">

  <rule id="WZ-AIPI001" level="9" group="ai,prompt-injection,direct">
    <description>AI — Direct Prompt Injection — Detects direct prompt injection attempts overriding system instructions via user</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-PI-001</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>prompt-injection|direct</match>
  </rule>

  <rule id="WZ-AIPI002" level="9" group="ai,prompt-injection,indirect">
    <description>AI — Indirect Prompt Injection via External Data — Detects prompt injection payloads embedded in uploaded files, web pages, or exte</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-PI-002</field>
    <mitre>
      <id>T1189</id>
    </mitre>
    <match>prompt-injection|indirect</match>
  </rule>

  <rule id="WZ-AIPI003" level="7" group="ai,prompt-extraction,system-prompt">
    <description>AI — System Prompt Extraction Attempt — Detects attempts to extract system prompts through direct questioning, translati</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-PI-003</field>
    <mitre>
      <id>T1083</id>
    </mitre>
    <match>prompt-extraction|system-prompt</match>
  </rule>

  <rule id="WZ-AIPI004" level="9" group="ai,jailbreak,role-play">
    <description>AI — Jailbreak via Role-Play Persona — Detects jailbreak attempts using role-play personas like DAN, developer mode, or</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-PI-004</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>jailbreak|role-play</match>
  </rule>

  <rule id="WZ-AIPI005" level="7" group="ai,prompt-injection,few-shot">
    <description>AI — Prompt Injection via Few-Shot Manipulation — Detects few-shot learning manipulation attacks that inject malicious examples in</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-PI-005</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>prompt-injection|few-shot</match>
  </rule>

  <rule id="WZ-AIDEX001" level="9" group="ai,data-exfiltration,output-leak">
    <description>AI — Data Exfiltration via Model Output — Detects LLM outputs containing PII, training data, or confidential information i</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-DEX-001</field>
    <mitre>
      <id>T1213</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-012</gdpr>
    </compliance>
    <match>data-exfiltration|output-leak</match>
  </rule>

  <rule id="WZ-AIDEX002" level="9" group="ai,data-extraction,training-data">
    <description>AI — Training Data Extraction Attack — Detects attempts to extract training data from LLM through targeted queries and </description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-DEX-002</field>
    <mitre>
      <id>T1213</id>
    </mitre>
    <match>data-extraction|training-data</match>
  </rule>

  <rule id="WZ-AIDEX003" level="9" group="ai,pii-leakage,llm-output">
    <description>AI — PII Leakage via LLM Response — Detects PII (SSN, email, phone, address) leaking through LLM responses</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-DEX-003</field>
    <mitre>
      <id>T1213</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-012</gdpr>
      <hipaa>HIPAA-TS-007</hipaa>
    </compliance>
    <match>pii-leakage|llm-output</match>
  </rule>

  <rule id="WZ-AIMDL001" level="9" group="ai,model-poisoning,training-data">
    <description>AI — Model Poisoning via Training Data Manipulation — Detects indicators of model poisoning through anomalous training data or unexpec</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-MDL-001</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>model-poisoning|training-data</match>
  </rule>

  <rule id="WZ-AIMDL002" level="9" group="ai,backdoor,model-poisoning">
    <description>AI — Backdoor Trigger Detection in LLM Output — Detects potential backdoor triggers in LLM output patterns that indicate comprom</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-MDL-002</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>backdoor|model-poisoning</match>
  </rule>

  <rule id="WZ-AIRAG001" level="7" group="ai,rag,data-poisoning">
    <description>AI — RAG Pipeline Data Poisoning — Detects injection of malicious content into RAG knowledge bases that corrupts re</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-RAG-001</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>rag|data-poisoning</match>
  </rule>

  <rule id="WZ-AIRAG002" level="7" group="ai,rag,adversarial-docs">
    <description>AI — RAG Retrieval Manipulation via Adversarial Documents — Detects adversarial documents in RAG stores designed to manipulate retrieval res</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-RAG-002</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>rag|adversarial-docs</match>
  </rule>

  <rule id="WZ-AIHAL001" level="5" group="ai,hallucination,fabrication">
    <description>AI — Hallucination Detection — Fabricated Entity References — Detects LLM responses containing fabricated URLs, citations, or entity reference</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-HAL-001</field>
    <mitre>
      <id></id>
    </mitre>
    <match>hallucination|fabrication</match>
  </rule>

  <rule id="WZ-AIHAL002" level="5" group="ai,hallucination,inconsistency">
    <description>AI — Hallucination — Inconsistent Factual Claims — Detects LLM outputs making contradictory factual claims across conversation turn</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-HAL-002</field>
    <mitre>
      <id></id>
    </mitre>
    <match>hallucination|inconsistency</match>
  </rule>

  <rule id="WZ-AIBIA001" level="7" group="ai,bias,fairness">
    <description>AI — Bias Detection — Demographic Disparities — Detects demographic bias patterns in LLM outputs including gender, racial, or et</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-BIA-001</field>
    <mitre>
      <id></id>
    </mitre>
    <match>bias|fairness</match>
  </rule>

  <rule id="WZ-AIGOV001" level="5" group="ai,governance,risk-assessment">
    <description>AI — Governance — Missing Risk Assessment — Detects AI system operations without documented risk assessment per EU AI Act Ar</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-GOV-001</field>
    <mitre>
      <id></id>
    </mitre>
    <match>governance|risk-assessment</match>
  </rule>

  <rule id="WZ-AIGOV002" level="9" group="ai,governance,human-oversight">
    <description>AI — Governance — High-Risk System Without Human Oversight — Detects high-risk AI system operations without required human oversight mechanis</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-GOV-002</field>
    <mitre>
      <id></id>
    </mitre>
    <match>governance|human-oversight</match>
  </rule>

  <rule id="WZ-AIGOV003" level="5" group="ai,governance,transparency">
    <description>AI — Governance — Missing Transparency Requirements — Detects AI system operations lacking required transparency disclosures per EU AI</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-GOV-003</field>
    <mitre>
      <id></id>
    </mitre>
    <match>governance|transparency</match>
  </rule>

  <rule id="WZ-AIRED001" level="9" group="ai,red-team,adversarial-suffix">
    <description>AI — Red Team — Adversarial Suffix Attack — Detects adversarial suffix attacks that append optimized suffixes to bypass LLM </description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-RED-001</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>red-team|adversarial-suffix</match>
  </rule>

  <rule id="WZ-AIRED002" level="7" group="ai,red-team,token-smuggling">
    <description>AI — Red Team — Token Smuggling Attack — Detects token smuggling attacks that use encoding or fragmentation to bypass LLM</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-RED-002</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>red-team|token-smuggling</match>
  </rule>

  <rule id="WZ-AIRED003" level="9" group="ai,red-team,multi-turn-jailbreak">
    <description>AI — Red Team — Multi-Turn Jailbreak — Detects multi-turn conversations designed to progressively jailbreak an LLM thro</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-RED-003</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>red-team|multi-turn-jailbreak</match>
  </rule>

  <rule id="WZ-AISAF001" level="9" group="ai,safety,harmful-content">
    <description>AI — Safety — Harmful Content Generation — Detects LLM attempts to generate harmful content including weapons, drugs, or da</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-SAF-001</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>safety|harmful-content</match>
  </rule>

  <rule id="WZ-AISAF002" level="9" group="ai,safety,self-harm">
    <description>AI — Safety — Self-Harm Content Facilitation — Detects LLM responses that may facilitate or encourage self-harm behavior</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-SAF-002</field>
    <mitre>
      <id></id>
    </mitre>
    <match>safety|self-harm</match>
  </rule>

  <rule id="WZ-AIVEC001" level="9" group="ai,vector-db,pinecone">
    <description>AI — Vector DB — Pinecone Namespace Isolation Bypass — Detects cross-namespace data access in Pinecone vector database</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-VEC-001</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-003</gdpr>
    </compliance>
    <match>vector-db|pinecone</match>
  </rule>

  <rule id="WZ-AIVEC002" level="9" group="ai,vector-db,weaviate">
    <description>AI — Vector DB — Weaviate Tenant Data Isolation Failure — Detects cross-tenant data access in Weaviate vector database</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-VEC-002</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-003</gdpr>
    </compliance>
    <match>vector-db|weaviate</match>
  </rule>

  <rule id="WZ-AIVEC003" level="7" group="ai,vector-db,chromadb">
    <description>AI — Vector DB — ChromaDB Embedding Poisoning — Detects adversarial embedding vectors inserted into ChromaDB collections</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-VEC-003</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>vector-db|chromadb</match>
  </rule>

  <rule id="WZ-AIVEC004" level="7" group="ai,vector-db,qdrant">
    <description>AI — Vector DB — Qdrant Payload Filter Injection — Detects filter injection attacks in Qdrant vector search payloads</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">AI-VEC-004</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <compliance>
      <pci_dss>6.5</pci_dss>
    </compliance>
    <match>vector-db|qdrant</match>
  </rule>

</group>