<!--
  Wazuh SIEM Alert Rules — api-security
  Total rules: 16
-->
<group name="wazuh,api,security">

  <rule id="WZ-API001" level="9" group="api,bola,idor">
    <description>API1: Broken Object Level Authorization (BOLA) — Detects sequential or unauthorized access to API objects by ID indicating broken</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-001</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <compliance>
      <pci_dss>8.5</pci_dss>
    </compliance>
    <match>bola|idor</match>
  </rule>

  <rule id="WZ-API002" level="9" group="api,authentication,default-credentials">
    <description>API2: Broken Authentication — Default Credentials — Detects API authentication attempts using default or well-known credentials</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-002</field>
    <mitre>
      <id>T1078.001</id>
    </mitre>
    <compliance>
      <pci_dss>2.1</pci_dss>
    </compliance>
    <match>authentication|default-credentials</match>
  </rule>

  <rule id="WZ-API003" level="7" group="api,bopla,property-level">
    <description>API3: Broken Object Property Level Authorization (BOPLA) — Detects API responses returning sensitive properties that the user should not ac</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-003</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-012</gdpr>
    </compliance>
    <match>bopla|property-level</match>
  </rule>

  <rule id="WZ-API004" level="7" group="api,rate-limiting,resource-consumption">
    <description>API4: Unrestricted Resource Consumption — Rate Limiting — Detects API requests exceeding rate limits indicating denial of service or scrap</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-004</field>
    <mitre>
      <id>T1498</id>
    </mitre>
    <match>rate-limiting|resource-consumption</match>
  </rule>

  <rule id="WZ-API005" level="9" group="api,flba,privilege-escalation">
    <description>API5: Broken Function Level Authorization — Detects access to administrative API endpoints by non-administrative users</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-005</field>
    <mitre>
      <id>T1548</id>
    </mitre>
    <compliance>
      <pci_dss>8.5</pci_dss>
    </compliance>
    <match>flba|privilege-escalation</match>
  </rule>

  <rule id="WZ-API006" level="7" group="api,business-flows,automation">
    <description>API6: Unrestricted Access to Sensitive Business Flows — Detects automated or excessive access to business-critical API flows like checko</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-006</field>
    <mitre>
      <id>T1498</id>
    </mitre>
    <match>business-flows|automation</match>
  </rule>

  <rule id="WZ-API007" level="9" group="api,ssrf,server-side">
    <description>API7: Server-Side Request Forgery via API — Detects SSRF attempts through API endpoints targeting internal networks or cloud</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-007</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <compliance>
      <pci_dss>6.5</pci_dss>
    </compliance>
    <match>ssrf|server-side</match>
  </rule>

  <rule id="WZ-API008" level="5" group="api,misconfiguration,swagger">
    <description>API8: Security Misconfiguration — Exposed API Documentation — Detects unauthorized access to API documentation endpoints like Swagger, OpenAPI</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-008</field>
    <mitre>
      <id>T1592.001</id>
    </mitre>
    <match>misconfiguration|swagger</match>
  </rule>

  <rule id="WZ-API009" level="7" group="api,shadow-api,inventory">
    <description>API9: Improper Inventory Management — Shadow API — Detects access to undocumented or deprecated API endpoints not in the API invent</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-009</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>shadow-api|inventory</match>
  </rule>

  <rule id="WZ-API010" level="5" group="api,unsafe-consumption,response-handling">
    <description>API10: Unsafe Consumption of API Responses — Detects API responses containing executable content or unsafe data that could ha</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-010</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>unsafe-consumption|response-handling</match>
  </rule>

  <rule id="WZ-API011" level="7" group="api,mass-assignment,parameter-tampering">
    <description>API — Mass Assignment via Parameter Tampering — Detects mass assignment attacks where request parameters include unauthorized fi</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-011</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <match>mass-assignment|parameter-tampering</match>
  </rule>

  <rule id="WZ-API012" level="7" group="api,graphql,dos">
    <description>API — GraphQL Query Depth Attack — Detects GraphQL queries with excessive depth or complexity indicating DoS or dat</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-012</field>
    <mitre>
      <id>T1498</id>
    </mitre>
    <match>graphql|dos</match>
  </rule>

  <rule id="WZ-API013" level="9" group="api,jwt,algorithm-none">
    <description>API — JWT Token None Algorithm Attack — Detects JWT tokens with algorithm set to none or missing algorithm indicating au</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-013</field>
    <mitre>
      <id>T1606</id>
    </mitre>
    <match>jwt|algorithm-none</match>
  </rule>

  <rule id="WZ-API014" level="7" group="api,data-exposure,pii">
    <description>API — Excessive Data Exposure in Response — Detects API responses returning more data than requested including PII, credenti</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-014</field>
    <mitre>
      <id>T1213</id>
    </mitre>
    <compliance>
      <gdpr>GDPR-25-012</gdpr>
    </compliance>
    <match>data-exposure|pii</match>
  </rule>

  <rule id="WZ-API015" level="7" group="api,nosql-injection,injection">
    <description>API — NoSQL Injection in Request Parameters — Detects NoSQL injection operators ($where, $regex, $gt) in API request parameter</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-015</field>
    <mitre>
      <id>T1190</id>
    </mitre>
    <match>nosql-injection|injection</match>
  </rule>

  <rule id="WZ-API016" level="9" group="api,api-keys,access-control">
    <description>API — Broken Access Control on API Keys — Detects API key usage from unauthorized sources or with excessive permissions</description>
    <decoded_as>web_log</decoded_as>
    <field name="test_id">API-016</field>
    <mitre>
      <id>T1078</id>
    </mitre>
    <match>api-keys|access-control</match>
  </rule>

</group>